// eslint-disable-next-line
import { shallow } from 'enzyme'
// eslint-disable-next-line
import LeftDashboardMarketPlace from '../leftDashboard'

jest.mock('../leftDashboard')

/**
 * @todo unit testing
 */
describe('Render Filter on Sidebar', () => {
  it('should render <LeftDashboard/>', () => {})
})
